name: CI/CD Pipeline

on:
  push:
     branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:  # Manual trigger

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests with coverage
        env:
          SECRET_KEY: test-secret-key-for-ci
          DEBUG: 'True'
          ALLOWED_HOSTS: localhost,127.0.0.1
          DATABASE_URL: sqlite:///test_db.sqlite3
        run: |
          pytest --cov=. --cov-report=term-missing --cov-report=xml

      - name: Upload coverage reports
        uses: codecov/codecov-action@v3
        if: always()
        with:
          file: ./coverage.xml
          fail_ci_if_error: false

  build-and-push:
    name: Build and Push Docker Image
    runs-on: ubuntu-latest
    needs: test
    if: (github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'workflow_dispatch'
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:buildcache
          cache-to: type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:buildcache,mode=max

  # deploy:
  #   name: Deploy to Server
  #   runs-on: ubuntu-latest
  #   needs: build-and-push
  #   if: (github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'workflow_dispatch'
  #   environment:
  #     name: production
  #     url: http://${{ secrets.DEPLOY_HOST }}

  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4

  #     - name: Set up SSH
  #       uses: webfactory/ssh-agent@v0.9.0
  #       with:
  #         ssh-private-key: ${{ secrets.DEPLOY_SSH_KEY }}

  #     - name: Add server to known hosts
  #       run: |
  #         ssh-keyscan -p ${{ secrets.DEPLOY_PORT || 22 }} ${{ secrets.DEPLOY_HOST }} >> ~/.ssh/known_hosts

  #     - name: Prepare deployment script
  #       run: |
  #         cat > deploy.sh << 'SCRIPT'
  #         #!/bin/bash
  #         set -e
          
  #         REGISTRY="$1"
  #         IMAGE_NAME="$2"
  #         GITHUB_USER="$3"
  #         GITHUB_TOKEN="$4"
          
  #         echo "Logging in to GitHub Container Registry..."
  #         echo "$GITHUB_TOKEN" | docker login "$REGISTRY" -u "$GITHUB_USER" --password-stdin
          
  #         echo "Pulling latest image..."
  #         docker pull "$REGISTRY/$IMAGE_NAME:latest"
          
  #         echo "Stopping existing container (if running)..."
  #         docker stop esign-backend || true
  #         docker rm esign-backend || true
          
  #         echo "Starting new container..."
  #         docker run -d \
  #           --name esign-backend \
  #           --restart unless-stopped \
  #           -p 8000:8000 \
  #           -e SECRET_KEY="$5" \
  #           -e DEBUG=False \
  #           -e ALLOWED_HOSTS="$6" \
  #           -e DATABASE_URL="$7" \
  #           -e CELERY_BROKER_URL="$8" \
  #           -e CACHE_LOCATION="$9" \
  #           "$REGISTRY/$IMAGE_NAME:latest"
          
  #         echo "Waiting for container to be healthy..."
  #         sleep 5
          
  #         echo "Checking container status..."
  #         docker ps | grep esign-backend || (echo "Container failed to start!" && exit 1)
          
  #         echo "Deployment completed successfully!"
  #         SCRIPT
  #         chmod +x deploy.sh

  #     - name: Deploy to server
  #       env:
  #         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN || secrets.GHCR_PAT }}
  #       run: |
  #         scp -P ${{ secrets.DEPLOY_PORT || 22 }} deploy.sh ${{ secrets.DEPLOY_USER }}@${{ secrets.DEPLOY_HOST }}:/tmp/deploy.sh
  #         ssh -p ${{ secrets.DEPLOY_PORT || 22 }} ${{ secrets.DEPLOY_USER }}@${{ secrets.DEPLOY_HOST }} \
  #           "bash /tmp/deploy.sh \
  #           ${{ env.REGISTRY }} \
  #           ${{ env.IMAGE_NAME }} \
  #           ${{ github.actor }} \
  #           ${{ secrets.GITHUB_TOKEN || secrets.GHCR_PAT }} \
  #           '${{ secrets.SECRET_KEY }}' \
  #           '${{ secrets.ALLOWED_HOSTS }}' \
  #           '${{ secrets.DATABASE_URL }}' \
  #           '${{ secrets.CELERY_BROKER_URL }}' \
  #           '${{ secrets.CACHE_LOCATION }}'"

  #     - name: Health check
  #       run: |
  #         sleep 10
  #         curl -f http://${{ secrets.DEPLOY_HOST }}:8000/health/ || exit 1
